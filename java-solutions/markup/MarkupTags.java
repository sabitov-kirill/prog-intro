package markup;

/**
 * Markup tags record, represents element tags for specific markup.
 *
 * @param begin begin element tag.
 * @param end   end element tag.
 */
public record MarkupTags(String begin, String end) {
}
